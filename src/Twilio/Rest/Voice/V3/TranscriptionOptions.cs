/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio API definition for public-api voice
 * Powers Twilio public-api voice
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


using System;
using System.Collections.Generic;
using Twilio.Base;
using Twilio.Converters;




namespace Twilio.Rest.Voice.V3
{

    /// <summary> create </summary>
    public class CreateTranscriptionOptions : IOptions<TranscriptionCreateResource>
    {
        
        
        public TranscriptionResource.CreateV3TranscriptionsRequest CreateV3TranscriptionsRequest { get; }

        ///<summary> A unique key to ensure idempotency. We recommend using UUID v7. Requests with the same key within the idempotency window return the original response. </summary> 
        public string IdempotencyKey { get; set; }


        /// <summary> Construct a new CreateV3TranscriptionsOptions </summary>
        /// <param name="createV3TranscriptionsRequest">  </param>
        public CreateTranscriptionOptions(TranscriptionResource.CreateV3TranscriptionsRequest createV3TranscriptionsRequest)
        {
            CreateV3TranscriptionsRequest = createV3TranscriptionsRequest;
        }

        
        /// <summary> Generate the request body </summary>
        public string GetBody()
        {
            string body = "";

            if (CreateV3TranscriptionsRequest != null)
            {
                body = TranscriptionResource.ToJson(CreateV3TranscriptionsRequest);
            }
            return body;
        }

        
    /// <summary> Generate the necessary header parameters </summary>
    public List<KeyValuePair<string, string>> GetHeaderParams()
    {
        var p = new List<KeyValuePair<string, string>>();
        if (IdempotencyKey != null)
        {
            p.Add(new KeyValuePair<string, string>("Idempotency-Key", IdempotencyKey));
        }
        return p;
    }

    }

    /// <summary> fetch </summary>
    public class FetchTranscriptionOptions : IOptions<TranscriptionFetchResource>
    {
    
        ///<summary> The unique identifier of the transcription to fetch </summary> 
        public string PathTranscriptionId { get; }



        /// <summary> Construct a new FetchTranscriptionOptions </summary>
        /// <param name="pathTranscriptionId"> The unique identifier of the transcription to fetch </param>
        public FetchTranscriptionOptions(string pathTranscriptionId)
        {
            PathTranscriptionId = pathTranscriptionId;
        }

        
        /// <summary> Generate the necessary parameters </summary>
        public List<KeyValuePair<string, string>> GetParams()
        {
            var p = new List<KeyValuePair<string, string>>();

            return p;
        }

    

    }


    /// <summary> Returns the account's transcriptions, newest first. Filters combine with AND, and paging uses the opaque cursors in meta rather than page numbers: pass meta.nextToken to advance and meta.previousToken to go back. A token is only valid for the filter set that produced it, so changing sourceId or the date range means starting a new walk. Items are the transcription resource itself. Create and fetch-by-id return the long running operation envelope instead, because creation is asynchronous. </summary>
    public class ReadTranscriptionOptions : ReadOptions<TranscriptionReadResource>
    {
    
        ///<summary> Only include transcriptions created at or after this time (inclusive) </summary> 
        public DateTime? CreatedAfter { get; set; }

        ///<summary> Only include transcriptions created strictly before this time (exclusive) </summary> 
        public DateTime? CreatedBefore { get; set; }

        ///<summary> Only include transcriptions whose resolved language matches this value exactly. The comparison is case sensitive, so use the stored form, for example en-US. </summary> 
        public string LanguageCode { get; set; }

        ///<summary> Only include transcriptions for this source audio. Must be a Recording SID in lowercase hex; anything else is rejected with a 400. </summary> 
        public string SourceId { get; set; }

        ///<summary> Only include transcriptions in this status </summary> 
        public TranscriptionResource.StatusEnum Status { get; set; }




        
        /// <summary> Generate the necessary parameters </summary>
        public List<KeyValuePair<string, string>> GetParams()
        {
            var p = new List<KeyValuePair<string, string>>();

            if (CreatedAfter != null)
            {
                p.Add(new KeyValuePair<string, string>("createdAfter", Serializers.DateTimeIso8601(CreatedAfter)));
            }
            if (CreatedBefore != null)
            {
                p.Add(new KeyValuePair<string, string>("createdBefore", Serializers.DateTimeIso8601(CreatedBefore)));
            }
            if (LanguageCode != null)
            {
                p.Add(new KeyValuePair<string, string>("languageCode", LanguageCode));
            }
            if (SourceId != null)
            {
                p.Add(new KeyValuePair<string, string>("sourceId", SourceId));
            }
            if (Status != null)
            {
                p.Add(new KeyValuePair<string, string>("status", Status.ToString()));
            }
            if (PageSize != null)
            {
                p.Add(new KeyValuePair<string, string>("pageSize", PageSize.ToString()));
            }
            return p;
        }

    

    }

}

